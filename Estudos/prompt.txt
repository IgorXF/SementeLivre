Tarefa — Protótipo de Autenticação Spring Security + JWT

A pasta atual está completamente vazia. Implemente esta tarefa do zero, criando toda a estrutura necessária para executar o projeto.

Implemente exatamente os requisitos abaixo.

Não adicione nenhuma funcionalidade, requisito, melhoria ou complexidade que não esteja descrita nesta tarefa.

Não remova nenhum requisito descrito.

Objetivo

Criar protótipo de autenticação funcional com Spring Security + JWT, demonstrando conceitos de segurança HTTP, filtros e hash de senhas.

O que estudar
Spring Security: o que é, como funciona a filter chain, como configurar SecurityFilterChain, @EnableWebSecurity
JWT (JSON Web Token): o que é, como funciona (header, payload, signature), como gerar e validar tokens, onde armazenar no cliente
Filtros HTTP: como criar um filtro customizado (JwtAuthenticationFilter), como interceptar requests antes do controller
BCrypt: o que é salting, como usar BCryptPasswordEncoder para hash de senhas, por que não usar MD5/SHA
O que entregar

Criar um protótipo "hello world JWT" com:

Endpoint POST /login que recebe email + senha e retorna JWT
Endpoint GET /me que retorna dados do usuário autenticado (protegido por JWT)
Endpoint POST /cadastrar que cria usuário com senha em BCrypt
Filtro JWT que valida o token em requests protegidos
Roles básicas PROPRIETARIO e ADMIN via JWT claims
Critérios de aceite
POST /cadastrar retorna 201 e salva senha com BCrypt
POST /login retorna JWT válido com credenciais corretas
GET /me sem token retorna 401
GET /me com token válido retorna dados do usuário
Token expira após tempo configurado
README documenta a estrutura do JWT e como testar
README

Criar um arquivo Markdown/README documentando:

a estrutura do JWT;
como testar a autenticação.
Referências

Utilizar como referência:

https://spring.io/guides/gs/securing-web/
https://jwt.io/introduction
https://docs.spring.io/spring-security/reference/servlet/authentication/passwords/bcrypt.html
Regra sobre comentários

Não adicionar comentários nos arquivos criados ou modificados.

Não utilizar comentários //, /* */ ou JavaDoc.

Toda explicação necessária deve estar no README.

Execução

Como a pasta está vazia, criar do zero tudo o que for necessário para executar e testar o protótipo.

Ao finalizar, verificar se o projeto está executável e se os critérios de aceite foram atendidos.

Não implementar nada além do que foi solicitado acima.